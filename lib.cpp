#include "lib.h"


class lib{

static Node getNode(int x, int y){
  
}; 
static void setNode(Node set,int x,int y){

  getNode(x,y) = set;
  
}; 

static void lib::retagN(*Node s, string tag);
static bool lib::untagN(*Node s, string tag);
static bool lib::tagN(*Node s, string tag);


}

